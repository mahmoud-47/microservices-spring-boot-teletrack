spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888   # Config Server address
      fail-fast: true              # Fail immediately if server is unavailable

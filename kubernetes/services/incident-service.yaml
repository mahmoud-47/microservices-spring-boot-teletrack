apiVersion: v1
kind: Service
metadata:
  name: incident-service
  namespace: teletrack360
spec:
  selector:
    app: incident-service
  ports:
    - port: 8082
      targetPort: 8082
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-service
  namespace: teletrack360
spec:
  replicas: 2
  selector:
    matchLabels:
      app: incident-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: incident-service
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            - sh
            - -c
            - until nc -z postgres 5432; do echo "Waiting for Postgres..."; sleep 2; done
        - name: wait-for-config
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until wget -qO- http://config-service:8888/actuator/health 2>/dev/null | grep -q UP; do
                echo "Waiting for config-service..."; sleep 5
              done
      containers:
        - name: incident-service
          image: mahmoudmhd47/teletrack-ms-incident-service:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: teletrack-secrets
          env:
            - name: SPRING_CONFIG_IMPORT
              value: configserver:http://config-service:8888
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://discovery-service:8761/eureka/
            - name: INCIDENT_DB_URL
              value: jdbc:postgresql://postgres:5432/incidents_db
            - name: INCIDENT_DB_USERNAME
              value: postgres
            - name: INCIDENT_DB_PASSWORD
              value: postgres
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: kafka:9092
            - name: TRACING_URL
              value: http://jaeger:4317
            - name: HIKARI_MAX_POOL_SIZE
              value: "10"
            - name: HIKARI_MIN_IDLE
              value: "5"
            - name: HIKARI_IDLE_TIMEOUT
              value: "600000"
            - name: HIKARI_CONNECTION_TIMEOUT
              value: "30000"
            - name: HIKARI_MAX_LIFETIME
              value: "1800000"
            - name: HIKARI_LEAK_DETECTION_THRESHOLD
              value: "2000"
            - name: TOMCAT_MAX_THREADS
              value: "200"
            - name: TOMCAT_MIN_SPARE_THREADS
              value: "10"
            - name: TOMCAT_ACCEPT_COUNT
              value: "100"
            - name: TOMCAT_MAX_CONNECTIONS
              value: "8192"
            - name: FEIGN_CB_ENABLED
              value: "true"
            - name: CB_SLIDING_WINDOW_SIZE
              value: "10"
            - name: CB_MIN_CALLS
              value: "5"
            - name: CB_FAILURE_RATE_THRESHOLD
              value: "50"
            - name: CB_WAIT_DURATION_OPEN_STATE
              value: 60s
            - name: CB_HALF_OPEN_PERMITTED_CALLS
              value: "3"
            - name: CB_SLOW_CALL_DURATION_THRESHOLD
              value: 2s
            - name: CB_SLOW_CALL_RATE_THRESHOLD
              value: "50"
            - name: LOGSTASH_HOST
              value: logstash
            - name: LOGSTASH_PORT
              value: "5000"
          ports:
            - containerPort: 8082
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 60
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
